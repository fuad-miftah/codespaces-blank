/// <reference types="node" />
import { GTX } from "../gtx/types";
/**
 *
 * Encodes the gtx using ASN.1.
 * @returns the gtx encoded
 */
export declare const encodeTransaction: (gtx: GTX) => Buffer;
